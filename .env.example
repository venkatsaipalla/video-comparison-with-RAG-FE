# Copy to `.env` at the project root (all API calls use this via src/lib/env.ts).
# Restart `npm run dev` after editing.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Timeout for POST /init and POST /chat (seconds). Omit = no explicit client timeout.
NEXT_PUBLIC_API_TIMEOUT_SEC=120
